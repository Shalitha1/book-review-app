PORT=3001
DB_HOST=your-rds-endpoint.amazonaws.com
DB_PORT=3306
DB_NAME=bookreview
DB_USER=admin
DB_PASS=replace-with-a-secret
JWT_SECRET=replace-with-a-long-random-secret
# Optional. Leave unset when requests arrive through the same-origin Nginx proxy.
# ALLOWED_ORIGINS=https://books.example.com
